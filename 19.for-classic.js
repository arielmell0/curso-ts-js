const myArray = ['ovo', 'farinha', 'cobertura', 'cereja']

//index = indíce, ele comeã em 0 (index = 0) e vai aumentando(index++) conforme for menor ou igual ao
//tamanho do lista(index <= myArray.length)
for(let index = 0; index < myArray.length; index++) {
    console.log(myArray[index])
}